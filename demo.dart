import 'package:flutter/material.dart';

class dashboard1 extends StatelessWidget {
  const dashboard1({super.key});

  @override
  Widget build(BuildContext context) {
    return 
    SafeArea( 
      child: Scaffold(
        
        body: Center(
          child: Text("Mudaliar Harini Ravi")),
    
    
        ),
    );
   }
}